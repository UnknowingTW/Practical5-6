{
  "name": "simple-nodejs-app",
  "version": "1.0.0",
  "description": "nodejs application",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "CI=false react-scripts build",
    "test": "react-scripts test --watchAll=false --ci --passWithNoTests",
    "test:coverage": "react-scripts test --watchAll=false --ci --coverage --passWithNoTests",
    "eject": "react-scripts eject",
    "deploy:prod": "echo 'Deploying to production environment...'",
    "deploy:stage": "echo 'Deploying to staging environment...'"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.17.3",
    "node-fetch": "^2.6.7",
    "nodemon": "^2.0.20",
    "request": "^2.88.2",
    "wiki-infobox-parser": "^0.1.11"
  },
  "devDependencies": {
    "react-scripts": "^5.0.1"
  }
}
